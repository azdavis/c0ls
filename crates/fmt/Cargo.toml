[package]
name = "fmt"
version = "0.1.0"
authors = ["Ariel Davis <ariel.z.davis@icloud.com>"]
edition = "2018"
publish = false

[lib]
doctest = false

[dependencies]
syntax = { path = "../syntax" }

[dev-dependencies]
lex = { path = "../lex" }
parse = { path = "../parse" }
