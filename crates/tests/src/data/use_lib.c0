#use <conio>
#use <string>
#use <util>

string foo(int x, int y) {
  string s = readline();
//         ^ hover: <none>
  if (eof()) {
//    ^^^^^ hover: bool
    return s;
//  ^^^^^^ hover: <none>
  }
  printint(x);
//^^^^^^^^^^^ hover: void
  printint(int_size());
//         ^^^^^^^^^^ hover: int
  printint(x + abs(x));
  printint(max(x, y));
  println(int2hex(x * y));
//^^^^^^^^^^^^^^^^^^^^^^^ hover: void
  flush();
  return string_join(s, int2hex(x ^ y));
//       ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^ hover: string
}
